import numpy as np
point=np.linspace(0,100,15)
print(point)

identidad=np.eye(5)
print(identidad)

aleatorio=np.random.randint(1,200,(2,2))
print(aleatorio)

arreglo=np.arange(9)
arreglo_reshaped=arreglo.reshape(3,3)
print(arreglo_reshaped)
